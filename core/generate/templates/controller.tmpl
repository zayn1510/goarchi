package controllers

import "github.com/gin-gonic/gin"

type {{.StructName}} struct{}

func (ctrl *{{.StructName}}) Index(c *gin.Context) {
	c.JSON(200, gin.H{
		"message": "Hello from {{.StructName}}",
	})
}
